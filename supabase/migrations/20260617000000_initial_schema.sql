-- Superseded by supabase/base.sql (complete schema v10).
-- Fresh projects: paste and run base.sql in the Supabase SQL editor.
-- CLI history only — no-op for linked projects that already applied this.

select 1;
